<h1 align="center">All Shows</h1>

<% flash.each do |type,message| %>
<%= message %>
<% end %>

<% @shows.each do |show| %>

<div align="center">
  <%= link_to "#{show.name}", show_path(show) %>
</div>

<% end %>

<div align="center">
  <%= link_to "Create New Show", new_show_path %>
</div>